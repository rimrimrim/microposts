<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div class="col-md-8">
    <%= link_to "followings", followings_user_path(@user) %>
    <%= link_to "followers", followers_user_path(@user) %>
    <%= render 'follow_form' if logged_in? %>
    <% if @followings.any? %>
      <h3>Your Followings (<%= @followings.count %>)</h3>
      <ol class="microposts">
        <hr>
        <%= render @followings %>
        <hr>
        <%= paginate @followings %>
      </ol>
    <% end %>
  </div>
</div>